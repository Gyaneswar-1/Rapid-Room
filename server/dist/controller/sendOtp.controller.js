export const sendMail = async (req, res) => {
    // get necessry info in body
    //email
    //generte the otp
    // generate th token using otp and email
    //store the token in the db users db
    // send the otp to the user
    return res.json({
        msg: "otp send successfully"
    });
};
